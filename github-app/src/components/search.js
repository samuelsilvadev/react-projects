import React from 'react'

const Search = () => (
    <div className="search">
        <input className="search__input" type="search" placeholder="Type a username to search at Github" />
    </div>
);

export default Search;
